#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_campaign class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Khalil
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 31 Aug 2020 17:32:22
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_campaign;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_campaignRepository : BaseDNetRepository<CRM_campaign>, ICRM_campaignRepository<CRM_campaign, int>
    {
        #region Constructor
        public CRM_campaignRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_campaign
        public ResponseMessage Create(CRM_campaign entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_campaignQuery.InsertCRM_campaign, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Campaign {0} has been successfully created", entity.campaignid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Campaign. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_campaign> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_campaign");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_campaign";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_campaign
        public ResponseMessage Update(CRM_campaign entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_campaignQuery.UpdateCRM_campaign, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Campaign{0} has been successfully updated", entity.campaignid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Campaign does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Campaign. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_campaign
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_campaign By Id
        public CRM_campaign Get(Guid campaignid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_campaign>(
                        CRM_campaignQuery.GetCRM_campaignByID, new { campaignid = campaignid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_campaign Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_campaign
        public List<CRM_campaign> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_campaign
        public List<CRM_campaign> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_campaign>();
        }
        #endregion

		#region Search CRM_campaign        
        public new List<CRM_campaign> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_campaign> result = SearchFetchPaging<CRM_campaign>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_campaign>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_campaignQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_campaign.campaignid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_campaignQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "campaignid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_campaign>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_campaign CRM_campaign)
        {
            CRM_campaign.createdbyname = UserLogin;
            CRM_campaign.createdon = DateTime.Now;
            CRM_campaign.RowStatus = "0";
            SetLastModifiedLog(CRM_campaign);
        }

        public void SetLastModifiedLog(CRM_campaign CRM_campaign)
        {
            CRM_campaign.modifiedbyname = UserLogin;
            CRM_campaign.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
