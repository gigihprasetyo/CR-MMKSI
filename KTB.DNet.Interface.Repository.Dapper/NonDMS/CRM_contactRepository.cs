#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_contact class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Ivan
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 15 Feb 2021 08:53:54
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_contact;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_contactRepository : BaseDNetRepository<CRM_contact>, ICRM_contactRepository<CRM_contact, int>
    {
        #region Constructor
        public CRM_contactRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_contact
        public ResponseMessage Create(CRM_contact entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_contactQuery.InsertCRM_contact, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Contact {0} has been successfully created", entity.contactid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Contact. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_contact> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_contact");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_contact";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_contact
        public ResponseMessage Update(CRM_contact entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_contactQuery.UpdateCRM_contact, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Contact{0} has been successfully updated", entity.contactid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Contact does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Contact. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_contact
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_contact By Id
        public CRM_contact Get(Guid contactid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_contact>(
                        CRM_contactQuery.GetCRM_contactByID, new { contactid = contactid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_contact Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_contact
        public List<CRM_contact> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_contact
        public List<CRM_contact> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_contact>();
        }
        #endregion

		#region Search CRM_contact        
        public new List<CRM_contact> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_contact> result = SearchFetchPaging<CRM_contact>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_contact>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_contactQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_contact.contactid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_contactQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "contactid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_contact>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_contact CRM_contact)
        {
            CRM_contact.createdbyname = UserLogin;
            CRM_contact.createdon = DateTime.Now;
            CRM_contact.RowStatus = "0";
            SetLastModifiedLog(CRM_contact);
        }

        public void SetLastModifiedLog(CRM_contact CRM_contact)
        {
            CRM_contact.modifiedbyname = UserLogin;
            CRM_contact.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
