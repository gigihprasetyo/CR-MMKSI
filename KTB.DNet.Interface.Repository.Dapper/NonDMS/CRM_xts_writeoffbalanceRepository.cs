#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_xts_writeoffbalance class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Khalil (version : v.1.08)
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 03 Sep 2020 13:18:19
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_xts_writeoffbalance;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_xts_writeoffbalanceRepository : BaseDNetRepository<CRM_xts_writeoffbalance>, ICRM_xts_writeoffbalanceRepository<CRM_xts_writeoffbalance, int>
    {
        #region Constructor
        public CRM_xts_writeoffbalanceRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_xts_writeoffbalance
        public ResponseMessage Create(CRM_xts_writeoffbalance entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_xts_writeoffbalanceQuery.InsertCRM_xts_writeoffbalance, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Writeoffbalance {0} has been successfully created", entity.xts_writeoffbalanceid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Writeoffbalance. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_xts_writeoffbalance> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_xts_writeoffbalance");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_xts_writeoffbalance";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_xts_writeoffbalance
        public ResponseMessage Update(CRM_xts_writeoffbalance entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_xts_writeoffbalanceQuery.UpdateCRM_xts_writeoffbalance, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Writeoffbalance{0} has been successfully updated", entity.xts_writeoffbalanceid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Writeoffbalance does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Writeoffbalance. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_xts_writeoffbalance
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_xts_writeoffbalance By Id
        public CRM_xts_writeoffbalance Get(Guid xts_writeoffbalanceid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_xts_writeoffbalance>(
                        CRM_xts_writeoffbalanceQuery.GetCRM_xts_writeoffbalanceByID, new { xts_writeoffbalanceid = xts_writeoffbalanceid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_xts_writeoffbalance Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_xts_writeoffbalance
        public List<CRM_xts_writeoffbalance> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_xts_writeoffbalance
        public List<CRM_xts_writeoffbalance> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_xts_writeoffbalance>();
        }
        #endregion

		#region Search CRM_xts_writeoffbalance        
        public new List<CRM_xts_writeoffbalance> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_xts_writeoffbalance> result = SearchFetchPaging<CRM_xts_writeoffbalance>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_xts_writeoffbalance>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_xts_writeoffbalanceQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_xts_writeoffbalance.xts_writeoffbalanceid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_xts_writeoffbalanceQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "xts_writeoffbalanceid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_xts_writeoffbalance>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_xts_writeoffbalance CRM_xts_writeoffbalance)
        {
            CRM_xts_writeoffbalance.createdbyname = UserLogin;
            CRM_xts_writeoffbalance.createdon = DateTime.Now;
            CRM_xts_writeoffbalance.RowStatus = "0";
            SetLastModifiedLog(CRM_xts_writeoffbalance);
        }

        public void SetLastModifiedLog(CRM_xts_writeoffbalance CRM_xts_writeoffbalance)
        {
            CRM_xts_writeoffbalance.modifiedbyname = UserLogin;
            CRM_xts_writeoffbalance.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
