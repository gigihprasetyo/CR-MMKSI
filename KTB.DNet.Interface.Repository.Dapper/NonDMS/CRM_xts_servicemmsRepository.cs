#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_xts_servicemms class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Ivan
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 19 Jan 2021 09:38:08
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_xts_servicemms;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_xts_servicemmsRepository : BaseDNetRepository<CRM_xts_servicemms>, ICRM_xts_servicemmsRepository<CRM_xts_servicemms, int>
    {
        #region Constructor
        public CRM_xts_servicemmsRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_xts_servicemms
        public ResponseMessage Create(CRM_xts_servicemms entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_xts_servicemmsQuery.InsertCRM_xts_servicemms, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Servicemms {0} has been successfully created", entity.xts_servicemmsid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Servicemms. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_xts_servicemms> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_xts_servicemms");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_xts_servicemms";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_xts_servicemms
        public ResponseMessage Update(CRM_xts_servicemms entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_xts_servicemmsQuery.UpdateCRM_xts_servicemms, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Servicemms{0} has been successfully updated", entity.xts_servicemmsid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Servicemms does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Servicemms. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_xts_servicemms
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_xts_servicemms By Id
        public CRM_xts_servicemms Get(Guid xts_servicemmsid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_xts_servicemms>(
                        CRM_xts_servicemmsQuery.GetCRM_xts_servicemmsByID, new { xts_servicemmsid = xts_servicemmsid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_xts_servicemms Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_xts_servicemms
        public List<CRM_xts_servicemms> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_xts_servicemms
        public List<CRM_xts_servicemms> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_xts_servicemms>();
        }
        #endregion

		#region Search CRM_xts_servicemms        
        public new List<CRM_xts_servicemms> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_xts_servicemms> result = SearchFetchPaging<CRM_xts_servicemms>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_xts_servicemms>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_xts_servicemmsQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_xts_servicemms.xts_servicemmsid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_xts_servicemmsQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "xts_servicemmsid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_xts_servicemms>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_xts_servicemms CRM_xts_servicemms)
        {
            CRM_xts_servicemms.createdbyname = UserLogin;
            CRM_xts_servicemms.createdon = DateTime.Now;
            CRM_xts_servicemms.RowStatus = 0;
            SetLastModifiedLog(CRM_xts_servicemms);
        }

        public void SetLastModifiedLog(CRM_xts_servicemms CRM_xts_servicemms)
        {
            CRM_xts_servicemms.modifiedbyname = UserLogin;
            CRM_xts_servicemms.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
