#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_xts_purchaseorder class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Khalil
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 01 Sep 2020 15:23:00
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_xts_purchaseorder;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_xts_purchaseorderRepository : BaseDNetRepository<CRM_xts_purchaseorder>, ICRM_xts_purchaseorderRepository<CRM_xts_purchaseorder, int>
    {
        #region Constructor
        public CRM_xts_purchaseorderRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_xts_purchaseorder
        public ResponseMessage Create(CRM_xts_purchaseorder entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_xts_purchaseorderQuery.InsertCRM_xts_purchaseorder, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Purchaseorder {0} has been successfully created", entity.xts_purchaseorderid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Purchaseorder. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_xts_purchaseorder> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_xts_purchaseorder");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_xts_purchaseorder";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_xts_purchaseorder
        public ResponseMessage Update(CRM_xts_purchaseorder entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_xts_purchaseorderQuery.UpdateCRM_xts_purchaseorder, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Purchaseorder{0} has been successfully updated", entity.xts_purchaseorderid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Purchaseorder does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Purchaseorder. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_xts_purchaseorder
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_xts_purchaseorder By Id
        public CRM_xts_purchaseorder Get(Guid xts_purchaseorderid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_xts_purchaseorder>(
                        CRM_xts_purchaseorderQuery.GetCRM_xts_purchaseorderByID, new { xts_purchaseorderid = xts_purchaseorderid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_xts_purchaseorder Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_xts_purchaseorder
        public List<CRM_xts_purchaseorder> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_xts_purchaseorder
        public List<CRM_xts_purchaseorder> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_xts_purchaseorder>();
        }
        #endregion

		#region Search CRM_xts_purchaseorder        
        public new List<CRM_xts_purchaseorder> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_xts_purchaseorder> result = SearchFetchPaging<CRM_xts_purchaseorder>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_xts_purchaseorder>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_xts_purchaseorderQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_xts_purchaseorder.xts_purchaseorderid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_xts_purchaseorderQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "xts_purchaseorderid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_xts_purchaseorder>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_xts_purchaseorder CRM_xts_purchaseorder)
        {
            CRM_xts_purchaseorder.createdbyname = UserLogin;
            CRM_xts_purchaseorder.createdon = DateTime.Now;
            CRM_xts_purchaseorder.RowStatus = "0";
            SetLastModifiedLog(CRM_xts_purchaseorder);
        }

        public void SetLastModifiedLog(CRM_xts_purchaseorder CRM_xts_purchaseorder)
        {
            CRM_xts_purchaseorder.modifiedbyname = UserLogin;
            CRM_xts_purchaseorder.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
