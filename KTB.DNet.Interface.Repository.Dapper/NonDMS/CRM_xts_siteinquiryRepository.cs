#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_xts_siteinquiry class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Ivan
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 19 Jan 2021 09:47:11
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_xts_siteinquiry;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_xts_siteinquiryRepository : BaseDNetRepository<CRM_xts_siteinquiry>, ICRM_xts_siteinquiryRepository<CRM_xts_siteinquiry, int>
    {
        #region Constructor
        public CRM_xts_siteinquiryRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_xts_siteinquiry
        public ResponseMessage Create(CRM_xts_siteinquiry entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_xts_siteinquiryQuery.InsertCRM_xts_siteinquiry, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Siteinquiry {0} has been successfully created", entity.xts_siteinquiryid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Siteinquiry. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_xts_siteinquiry> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_xts_siteinquiry");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_xts_siteinquiry";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_xts_siteinquiry
        public ResponseMessage Update(CRM_xts_siteinquiry entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_xts_siteinquiryQuery.UpdateCRM_xts_siteinquiry, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Siteinquiry{0} has been successfully updated", entity.xts_siteinquiryid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Siteinquiry does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Siteinquiry. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_xts_siteinquiry
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_xts_siteinquiry By Id
        public CRM_xts_siteinquiry Get(Guid xts_siteinquiryid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_xts_siteinquiry>(
                        CRM_xts_siteinquiryQuery.GetCRM_xts_siteinquiryByID, new { xts_siteinquiryid = xts_siteinquiryid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_xts_siteinquiry Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_xts_siteinquiry
        public List<CRM_xts_siteinquiry> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_xts_siteinquiry
        public List<CRM_xts_siteinquiry> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_xts_siteinquiry>();
        }
        #endregion

		#region Search CRM_xts_siteinquiry        
        public new List<CRM_xts_siteinquiry> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_xts_siteinquiry> result = SearchFetchPaging<CRM_xts_siteinquiry>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_xts_siteinquiry>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_xts_siteinquiryQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_xts_siteinquiry.xts_siteinquiryid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_xts_siteinquiryQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "xts_siteinquiryid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_xts_siteinquiry>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_xts_siteinquiry CRM_xts_siteinquiry)
        {
            CRM_xts_siteinquiry.createdbyname = UserLogin;
            CRM_xts_siteinquiry.createdon = DateTime.Now;
            CRM_xts_siteinquiry.RowStatus = 0;
            SetLastModifiedLog(CRM_xts_siteinquiry);
        }

        public void SetLastModifiedLog(CRM_xts_siteinquiry CRM_xts_siteinquiry)
        {
            CRM_xts_siteinquiry.modifiedbyname = UserLogin;
            CRM_xts_siteinquiry.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
