#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_serviceappointment class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Ivan
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 19 Jan 2021 09:35:16
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_serviceappointment;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_serviceappointmentRepository : BaseDNetRepository<CRM_serviceappointment>, ICRM_serviceappointmentRepository<CRM_serviceappointment, int>
    {
        #region Constructor
        public CRM_serviceappointmentRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_serviceappointment
        public ResponseMessage Create(CRM_serviceappointment entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_serviceappointmentQuery.InsertCRM_serviceappointment, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Serviceappointment {0} has been successfully created", entity.activityid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Serviceappointment. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_serviceappointment> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_serviceappointment");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_serviceappointment";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_serviceappointment
        public ResponseMessage Update(CRM_serviceappointment entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_serviceappointmentQuery.UpdateCRM_serviceappointment, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Serviceappointment{0} has been successfully updated", entity.activityid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Serviceappointment does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Serviceappointment. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_serviceappointment
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_serviceappointment By Id
        public CRM_serviceappointment Get(Guid activityid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_serviceappointment>(
                        CRM_serviceappointmentQuery.GetCRM_serviceappointmentByID, new { activityid = activityid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_serviceappointment Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_serviceappointment
        public List<CRM_serviceappointment> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_serviceappointment
        public List<CRM_serviceappointment> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_serviceappointment>();
        }
        #endregion

		#region Search CRM_serviceappointment        
        public new List<CRM_serviceappointment> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_serviceappointment> result = SearchFetchPaging<CRM_serviceappointment>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_serviceappointment>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_serviceappointmentQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_serviceappointment.activityid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_serviceappointmentQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "activityid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_serviceappointment>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_serviceappointment CRM_serviceappointment)
        {
            CRM_serviceappointment.createdbyname = UserLogin;
            CRM_serviceappointment.createdon = DateTime.Now;
            CRM_serviceappointment.RowStatus = 0;
            SetLastModifiedLog(CRM_serviceappointment);
        }

        public void SetLastModifiedLog(CRM_serviceappointment CRM_serviceappointment)
        {
            CRM_serviceappointment.modifiedbyname = UserLogin;
            CRM_serviceappointment.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
