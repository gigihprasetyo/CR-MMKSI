#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_xts_inventorytransaction class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Kemin
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 01 Sep 2020 11:50:48
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_xts_inventorytransaction;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_xts_inventorytransactionRepository : BaseDNetRepository<CRM_xts_inventorytransaction>, ICRM_xts_inventorytransactionRepository<CRM_xts_inventorytransaction, int>
    {
        #region Constructor
        public CRM_xts_inventorytransactionRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_xts_inventorytransaction
        public ResponseMessage Create(CRM_xts_inventorytransaction entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_xts_inventorytransactionQuery.InsertCRM_xts_inventorytransaction, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Inventorytransaction {0} has been successfully created", entity.xts_inventorytransactionid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Inventorytransaction. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_xts_inventorytransaction> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_xts_inventorytransaction");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_xts_inventorytransaction";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_xts_inventorytransaction
        public ResponseMessage Update(CRM_xts_inventorytransaction entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_xts_inventorytransactionQuery.UpdateCRM_xts_inventorytransaction, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Inventorytransaction{0} has been successfully updated", entity.xts_inventorytransactionid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Inventorytransaction does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Inventorytransaction. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_xts_inventorytransaction
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_xts_inventorytransaction By Id
        public CRM_xts_inventorytransaction Get(Guid xts_inventorytransactionid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_xts_inventorytransaction>(
                        CRM_xts_inventorytransactionQuery.GetCRM_xts_inventorytransactionByID, new { xts_inventorytransactionid = xts_inventorytransactionid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_xts_inventorytransaction Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_xts_inventorytransaction
        public List<CRM_xts_inventorytransaction> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_xts_inventorytransaction
        public List<CRM_xts_inventorytransaction> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_xts_inventorytransaction>();
        }
        #endregion

		#region Search CRM_xts_inventorytransaction        
        public new List<CRM_xts_inventorytransaction> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_xts_inventorytransaction> result = SearchFetchPaging<CRM_xts_inventorytransaction>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_xts_inventorytransaction>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_xts_inventorytransactionQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_xts_inventorytransaction.xts_inventorytransactionid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_xts_inventorytransactionQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "xts_inventorytransactionid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_xts_inventorytransaction>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_xts_inventorytransaction CRM_xts_inventorytransaction)
        {
            CRM_xts_inventorytransaction.createdbyname = UserLogin;
            CRM_xts_inventorytransaction.createdon = DateTime.Now;
            CRM_xts_inventorytransaction.RowStatus = "0";
            SetLastModifiedLog(CRM_xts_inventorytransaction);
        }

        public void SetLastModifiedLog(CRM_xts_inventorytransaction CRM_xts_inventorytransaction)
        {
            CRM_xts_inventorytransaction.modifiedbyname = UserLogin;
            CRM_xts_inventorytransaction.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
