#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_team class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Kemin
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 21 Dec 2020 17:16:00
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_team;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_teamRepository : BaseDNetRepository<CRM_team>, ICRM_teamRepository<CRM_team, int>
    {
        #region Constructor
        public CRM_teamRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion


        #region Create CRM_team
        public ResponseMessage Create(CRM_team entity)
        {

            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_teamQuery.InsertCRM_team, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Team {0} has been successfully created", entity.teamid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Team. " + GetInnerException(ex).Message;
            }

            return responseMessage;

        }
        #endregion


        public bool BulkInsert(List<CRM_team> data)
        {
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_team");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_team";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }


        #region Update CRM_team
        public ResponseMessage Update(CRM_team entity)
        {

            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_teamQuery.UpdateCRM_team, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Team{0} has been successfully updated", entity.teamid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Team does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Team. " + GetInnerException(ex).Message;
            }

            return responseMessage;

        }
        #endregion


        #region Delete CRM_team
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

        #region Get CRM_team By Id
        public CRM_team Get(Guid teamid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_team>(
                        CRM_teamQuery.GetCRM_teamByID, new { teamid = teamid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_team Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_team
        public List<CRM_team> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_team
        public List<CRM_team> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_team>();
        }
        #endregion

        #region Search CRM_team        
        public new List<CRM_team> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_team> result = SearchFetchPaging<CRM_team>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_team>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_teamQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_team.teamid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_teamQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "teamid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_team>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_team CRM_team)
        {
            CRM_team.createdbyname = UserLogin;
            CRM_team.createdon = DateTime.Now;
            CRM_team.RowStatus = "0";
            SetLastModifiedLog(CRM_team);
        }

        public void SetLastModifiedLog(CRM_team CRM_team)
        {
            CRM_team.modifiedbyname = UserLogin;
            CRM_team.modifiedon = DateTime.Now;
        }


        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }


    }
}
