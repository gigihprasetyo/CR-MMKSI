#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_xid_documentregistration class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Khalil (version : v.1.08)
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 04 Sep 2020 14:12:48
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_xid_documentregistration;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_xid_documentregistrationRepository : BaseDNetRepository<CRM_xid_documentregistration>, ICRM_xid_documentregistrationRepository<CRM_xid_documentregistration, int>
    {
        #region Constructor
        public CRM_xid_documentregistrationRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_xid_documentregistration
        public ResponseMessage Create(CRM_xid_documentregistration entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_xid_documentregistrationQuery.InsertCRM_xid_documentregistration, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Documentregistration {0} has been successfully created", entity.xid_documentregistrationid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Documentregistration. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_xid_documentregistration> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_xid_documentregistration");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_xid_documentregistration";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_xid_documentregistration
        public ResponseMessage Update(CRM_xid_documentregistration entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_xid_documentregistrationQuery.UpdateCRM_xid_documentregistration, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Documentregistration{0} has been successfully updated", entity.xid_documentregistrationid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Documentregistration does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Documentregistration. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_xid_documentregistration
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_xid_documentregistration By Id
        public CRM_xid_documentregistration Get(Guid xid_documentregistrationid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_xid_documentregistration>(
                        CRM_xid_documentregistrationQuery.GetCRM_xid_documentregistrationByID, new { xid_documentregistrationid = xid_documentregistrationid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_xid_documentregistration Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_xid_documentregistration
        public List<CRM_xid_documentregistration> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_xid_documentregistration
        public List<CRM_xid_documentregistration> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_xid_documentregistration>();
        }
        #endregion

		#region Search CRM_xid_documentregistration        
        public new List<CRM_xid_documentregistration> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_xid_documentregistration> result = SearchFetchPaging<CRM_xid_documentregistration>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_xid_documentregistration>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_xid_documentregistrationQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_xid_documentregistration.xid_documentregistrationid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_xid_documentregistrationQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "xid_documentregistrationid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_xid_documentregistration>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_xid_documentregistration CRM_xid_documentregistration)
        {
            CRM_xid_documentregistration.createdbyname = UserLogin;
            CRM_xid_documentregistration.createdon = DateTime.Now;
            CRM_xid_documentregistration.RowStatus = "0";
            SetLastModifiedLog(CRM_xid_documentregistration);
        }

        public void SetLastModifiedLog(CRM_xid_documentregistration CRM_xid_documentregistration)
        {
            CRM_xid_documentregistration.modifiedbyname = UserLogin;
            CRM_xid_documentregistration.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
