#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_equipment class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Ivan
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 18 Jan 2021 16:07:58
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_equipment;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_equipmentRepository : BaseDNetRepository<CRM_equipment>, ICRM_equipmentRepository<CRM_equipment, int>
    {
        #region Constructor
        public CRM_equipmentRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_equipment
        public ResponseMessage Create(CRM_equipment entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_equipmentQuery.InsertCRM_equipment, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Equipment {0} has been successfully created", entity.equipmentid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Equipment. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_equipment> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_equipment");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_equipment";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_equipment
        public ResponseMessage Update(CRM_equipment entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_equipmentQuery.UpdateCRM_equipment, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Equipment{0} has been successfully updated", entity.equipmentid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Equipment does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Equipment. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_equipment
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_equipment By Id
        public CRM_equipment Get(Guid equipmentid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_equipment>(
                        CRM_equipmentQuery.GetCRM_equipmentByID, new { equipmentid = equipmentid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_equipment Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_equipment
        public List<CRM_equipment> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_equipment
        public List<CRM_equipment> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_equipment>();
        }
        #endregion

		#region Search CRM_equipment        
        public new List<CRM_equipment> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_equipment> result = SearchFetchPaging<CRM_equipment>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_equipment>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_equipmentQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_equipment.equipmentid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_equipmentQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "equipmentid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_equipment>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_equipment CRM_equipment)
        {
            CRM_equipment.createdbyname = UserLogin;
            CRM_equipment.createdon = DateTime.Now;
            CRM_equipment.RowStatus = 0;
            SetLastModifiedLog(CRM_equipment);
        }

        public void SetLastModifiedLog(CRM_equipment CRM_equipment)
        {
            CRM_equipment.modifiedbyname = UserLogin;
            CRM_equipment.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
