#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_appointment class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Ivan
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 19 Jan 2021 09:14:36
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_appointment;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_appointmentRepository : BaseDNetRepository<CRM_appointment>, ICRM_appointmentRepository<CRM_appointment, int>
    {
        #region Constructor
        public CRM_appointmentRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_appointment
        public ResponseMessage Create(CRM_appointment entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_appointmentQuery.InsertCRM_appointment, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Appointment {0} has been successfully created", entity.activityid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Appointment. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_appointment> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_appointment");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_appointment";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_appointment
        public ResponseMessage Update(CRM_appointment entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_appointmentQuery.UpdateCRM_appointment, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Appointment{0} has been successfully updated", entity.activityid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Appointment does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Appointment. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_appointment
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_appointment By Id
        public CRM_appointment Get(Guid activityid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_appointment>(
                        CRM_appointmentQuery.GetCRM_appointmentByID, new { activityid = activityid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_appointment Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_appointment
        public List<CRM_appointment> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_appointment
        public List<CRM_appointment> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_appointment>();
        }
        #endregion

		#region Search CRM_appointment        
        public new List<CRM_appointment> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_appointment> result = SearchFetchPaging<CRM_appointment>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_appointment>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_appointmentQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_appointment.activityid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_appointmentQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "activityid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_appointment>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_appointment CRM_appointment)
        {
            CRM_appointment.createdbyname = UserLogin;
            CRM_appointment.createdon = DateTime.Now;
            CRM_appointment.RowStatus = 0;
            SetLastModifiedLog(CRM_appointment);
        }

        public void SetLastModifiedLog(CRM_appointment CRM_appointment)
        {
            CRM_appointment.modifiedbyname = UserLogin;
            CRM_appointment.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
