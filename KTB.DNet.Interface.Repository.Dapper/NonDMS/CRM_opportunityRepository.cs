#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_opportunity class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Kemin
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 31 Aug 2020 11:28:47
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_opportunity;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_opportunityRepository : BaseDNetRepository<CRM_Opportunity>, ICRM_opportunityRepository<CRM_Opportunity, int>
    {
        #region Constructor
        public CRM_opportunityRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_opportunity
        public ResponseMessage Create(CRM_Opportunity entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_opportunityQuery.InsertCRM_opportunity, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Prospect {0} has been successfully created", entity.opportunityid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Prospect. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_Opportunity> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_opportunity");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_opportunity";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_opportunity
        public ResponseMessage Update(CRM_Opportunity entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_opportunityQuery.UpdateCRM_opportunity, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Prospect{0} has been successfully updated", entity.opportunityid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Prospect does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Prospect. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_opportunity
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_opportunity By Id
        public CRM_Opportunity Get(Guid opportunityid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_Opportunity>(
                        CRM_opportunityQuery.GetCRM_opportunityByID, new { opportunityid = opportunityid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_Opportunity Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_opportunity
        public List<CRM_Opportunity> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_opportunity
        public List<CRM_Opportunity> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_Opportunity>();
        }
        #endregion

		#region Search CRM_opportunity        
        public new List<CRM_Opportunity> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_Opportunity> result = SearchFetchPaging<CRM_Opportunity>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_Opportunity>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_opportunityQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_opportunity.opportunityid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_opportunityQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "opportunityid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_Opportunity>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_Opportunity CRM_opportunity)
        {
            CRM_opportunity.createdbyname = UserLogin;
            CRM_opportunity.createdon = DateTime.Now;
            CRM_opportunity.RowStatus = 0;
            SetLastModifiedLog(CRM_opportunity);
        }

        public void SetLastModifiedLog(CRM_Opportunity CRM_opportunity)
        {
            CRM_opportunity.modifiedbyname = UserLogin;
            CRM_opportunity.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
