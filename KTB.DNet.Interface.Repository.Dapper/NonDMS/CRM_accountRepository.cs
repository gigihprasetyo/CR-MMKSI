#region Summary
/*
 ===========================================================================
 AUTHOR        : BSI Code Generator Interface 
 PURPOSE       : CRM_account class
 SPECIAL NOTES : Generated from database BSIDMS_MMKSI_IF_ACC_DEV
 GENERATED BY  : Nando
 ---------------------
 Copyright  (c) 2020 
 ---------------------
 $History      : $
 Created on 31 Aug 2020 17:24:46
 ===========================================================================
*/
#endregion

#region Namespace Imports
using Dapper;
using KTB.DNet.Interface.Domain;
using KTB.DNet.Interface.Framework;
using KTB.DNet.Interface.Repository.Dapper.NonDMS.SqlQuery.CRM_account;
using KTB.DNet.Interface.Repository.Interface;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Data;
using System.Data.SqlClient;
using System.Reflection;
#endregion

namespace KTB.DNet.Interface.Repository.Dapper.NonDMS
{
    public class CRM_accountRepository : BaseDNetRepository<CRM_account>, ICRM_accountRepository<CRM_account, int>
    {
        #region Constructor
        public CRM_accountRepository(string connectionString)
            : base(connectionString)
        { }
        #endregion

		
        #region Create CRM_account
        public ResponseMessage Create(CRM_account entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                //SetCreatedLog(entity);
                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    return connection.Execute(CRM_accountQuery.InsertCRM_account, entity, transaction);
                });

                responseMessage.Success = true;
                responseMessage.Status = ResponseStatus.Success;
                responseMessage.Message = string.Format("Customer {0} has been successfully created", entity.accountid);
                responseMessage.Data = entity;
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to create Customer. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		
		
        public bool BulkInsert(List<CRM_account> data)
        {	
            bool result = false;
            try
            {
                DataTable dataTableData = new DataTable("CRM_account");
                dataTableData = data.ToDataTableForCreate();
                dataTableData = UpdateEmptyGuid(dataTableData);
                int batchSize = AppConfigs.GetInt(Constants.DBInsertBatchSizeConfigName);

                ExecuteTransaction(Connection, (connection, transaction) =>
                {
                    using (SqlBulkCopy insert = new SqlBulkCopy((SqlConnection)connection, SqlBulkCopyOptions.Default, (SqlTransaction)transaction))
                    {
                        if (batchSize > 0)
                        {
                            insert.BatchSize = batchSize;
                        }
                        insert.DestinationTableName = "CRM_account";

                        insert.WriteToServer(dataTableData);

                        result = true;
                        return true;
                    }
                });

                return result;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }
		

        #region Update CRM_account
        public ResponseMessage Update(CRM_account entity)
        {
			
            ResponseMessage responseMessage = new ResponseMessage() { Success = false };

            try
            {
                if (entity != null)
                {
                    //SetLastModifiedLog(entity);
                    ExecuteTransaction(Connection, (connection, transaction) =>
                    {
                        return connection.Execute(CRM_accountQuery.UpdateCRM_account, entity, transaction);
                    });

                    responseMessage.Success = true;
                    responseMessage.Status = ResponseStatus.Success;
                    responseMessage.Message = string.Format("Customer{0} has been successfully updated", entity.accountid);
                    responseMessage.Data = entity;
                }
                else
                {
                    responseMessage.Status = ResponseStatus.Warning;
                    responseMessage.Message = "Customer does not exist";
                }
            }
            catch (Exception ex)
            {
                responseMessage.Status = ResponseStatus.Error;
                responseMessage.Message = "Failed to update Customer. " + GetInnerException(ex).Message;
            }

            return responseMessage;
			
        }
        #endregion
		

        #region Delete CRM_account
        public ResponseMessage Delete(int id)
        {
            return null;
        }
        #endregion

		#region Get CRM_account By Id
        public CRM_account Get(Guid accountid)
        {
            try
            {
                using (var cn = Connection)
                {
                    return cn.Query<CRM_account>(
                        CRM_accountQuery.GetCRM_accountByID, new { accountid = accountid }
                        ).SingleOrDefault();
                }
            }
            catch (Exception)
            {
                return null;
            }
        }

        public CRM_account Get(int id)
        {
            return null;
        }
        #endregion

        #region Get All CRM_account
        public List<CRM_account> GetAll()
        {
            return null;
        }
        #endregion

        #region Search CRM_account
        public List<CRM_account> Search(DataTablePostModel model, out int filteredResultsCount, out int totalResultsCount)
        {
            filteredResultsCount = 0;
            totalResultsCount = 0;
            return new List<CRM_account>();
        }
        #endregion

		#region Search CRM_account        
        public new List<CRM_account> Search(string strCriteria, string strInnerCriteria, string sortColumns, int page, int pageSize, out int filteredResultsCount, out int totalResultsCount)
        {
            try
            {
                string orderBy = sortColumns != string.Empty ? sortColumns.ToString() : null;
                filteredResultsCount = 0;

                List<CRM_account> result = SearchFetchPaging<CRM_account>((connection, query, sqlParams) =>
                {
                    return connection.Query<CRM_account>(query, sqlParams, null, true, Timeout, null).ToList();
                }, Connection, string.Format(CRM_accountQuery.SelectQuery,
                                                strCriteria,
                                                strInnerCriteria)
                , "CRM_account.accountid", null, orderBy, out filteredResultsCount, page, pageSize,
                string.Format(CRM_accountQuery.GetTotalQuery,
                                                strCriteria,
                                                strInnerCriteria), "accountid");

                totalResultsCount = filteredResultsCount;

                return result;

            }
            catch (Exception ex)
            {
                filteredResultsCount = 0;
                totalResultsCount = 0;
                return new List<CRM_account>();
            }
        }
        #endregion

        public void SetCreatedLog(CRM_account CRM_account)
        {
            CRM_account.createdbyname = UserLogin;
            CRM_account.createdon = DateTime.Now;
            CRM_account.RowStatus = "0";
            SetLastModifiedLog(CRM_account);
        }

        public void SetLastModifiedLog(CRM_account CRM_account)
        {
            CRM_account.modifiedbyname = UserLogin;
            CRM_account.modifiedon = DateTime.Now;
        }

		
        private DataTable UpdateEmptyGuid(DataTable dt)
        {
            foreach (DataColumn col in dt.Columns)
            {
                if (col.DataType.Name.ToString() == "Guid")
                {
                    foreach (DataRow row in dt.Rows)
                    {
                        if (row[col.ColumnName].ToString() == Guid.Empty.ToString())
                        {
                            row[col.ColumnName] = DBNull.Value;
                        }
                    }
                }
            }
            return dt;
        }
		

    }
}
