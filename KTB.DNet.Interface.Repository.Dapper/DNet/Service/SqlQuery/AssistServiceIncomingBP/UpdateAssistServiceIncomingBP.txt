UPDATE AssistServiceIncomingBP
	SET 
		AssistUploadLogID			= ISNULL(@AssistUploadLogID, AssistUploadLogID)
		, ChassisMasterID			= ISNULL(@ChassisMasterID, ChassisMasterID)
		, CustomerOwnerName			= ISNULL(@CustomerOwnerName, CustomerOwnerName)
		, CustomerOwnerPhoneNumber  = ISNULL(@CustomerOwnerPhoneNumber, CustomerOwnerPhoneNumber)
		, CustomerVisitName			= ISNULL(@CustomerVisitName, CustomerVisitName)
		, CustomerVisitPhoneNumber	= ISNULL(@CustomerVisitPhoneNumber, CustomerVisitPhoneNumber)
		, DamageCategory			= ISNULL(@DamageCategory, DamageCategory)
		, DealerCode				= ISNULL(@DealerCode, DealerCode)
		, DealerID					= ISNULL(@DealerID, DealerID)
		, InsuranceName				= ISNULL(@InsuranceName, InsuranceName)
		, KMService					= ISNULL(@KMService, KMService)
		, KodeChassis				= ISNULL(@KodeChassis, KodeChassis)
		, KodeMekanik				= ISNULL(@KodeMekanik, KodeMekanik)
		, MethodofPayment			= ISNULL(@MethodofPayment, MethodofPayment)
		, NoWorkOrder				= ISNULL(@NoWorkOrder, NoWorkOrder)
		, RemarksBM					= ISNULL(@RemarksBM, RemarksBM)
		, RemarksSpecial			= ISNULL(@RemarksSpecial, RemarksSpecial)
		, RemarksSystem				= ISNULL(@RemarksSystem, RemarksSystem)
		, RowStatus					= ISNULL(@RowStatus, RowStatus)
		, ServiceBooking			= ISNULL(@ServiceBooking, ServiceBooking)
		, ServiceTypeCode			= ISNULL(@ServiceTypeCode, ServiceTypeCode)
		, ServiceTypeID				= ISNULL(@ServiceTypeID, ServiceTypeID)
		, StatusAktif				= ISNULL(@StatusAktif, StatusAktif)
		, TglBukaTransaksi			= ISNULL(@TglBukaTransaksi, TglBukaTransaksi)
		, TglJanjiSelesai			= ISNULL(@TglJanjiSelesai, TglJanjiSelesai)
		, TglPengajuanEstimasi		= ISNULL(@TglPengajuanEstimasi, TglPengajuanEstimasi)
		, TglPersetujuanEstimasi	= ISNULL(@TglPersetujuanEstimasi, TglPersetujuanEstimasi)
		, TglTutupTransaksi			= ISNULL(@TglTutupTransaksi, TglTutupTransaksi)
		, TotalCat					= ISNULL(@TotalCat, TotalCat)
		, TotalLC					= ISNULL(@TotalLC, TotalLC)
		, TotalNonCat				= ISNULL(@TotalNonCat, TotalNonCat)
		, TotalPanel				= ISNULL(@TotalPanel, TotalPanel)
		, TotalSubOrder				= ISNULL(@TotalSubOrder, TotalSubOrder)
		, TrTraineMekanikID			= ISNULL(@TrTraineMekanikID, TrTraineMekanikID)
		, ValidateSystemStatus		= ISNULL(@ValidateSystemStatus, ValidateSystemStatus)
		, VehicleColorDesc			= ISNULL(@VehicleColorDesc, VehicleColorDesc)
		, VehicleModelDesc			= ISNULL(@VehicleModelDesc, VehicleModelDesc)
		, WaktuKeluar				= ISNULL(@WaktuKeluar, WaktuKeluar)
		, WaktuMasuk				= ISNULL(@WaktuMasuk, WaktuMasuk)
		, WorkOrderCategoryCode		= ISNULL(@WorkOrderCategoryCode, WorkOrderCategoryCode)
		, WorkOrderCategoryID		= ISNULL(@WorkOrderCategoryID, WorkOrderCategoryID)
		, WOStatus					= ISNULL(@WOStatus, WOStatus)
		, LastUpdateBy				= ISNULL(@LastUpdateBy, LastUpdateBy)
		, LastUpdateTime			= ISNULL(@LastUpdateTime, LastUpdateTime)
WHERE ID = @ID